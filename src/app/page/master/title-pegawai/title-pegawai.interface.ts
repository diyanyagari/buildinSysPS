import { EventEmitter } from '@angular/core';

export interface TitlePegawai {

  kode?: any,
  kodeExternal?: string,
  namaExternal?: string,
  namaTitlePegawai?: string,
  reportDisplay?: string,
  statusEnabled?: true,
  version?: 0,

}
