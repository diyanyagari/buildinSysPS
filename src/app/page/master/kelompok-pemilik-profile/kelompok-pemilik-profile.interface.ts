import { EventEmitter } from '@angular/core';

export interface KelompokPemilikProfile {

  kelompokPemilik?: string,
  kode?: any,
  kodeExternal?: string,
  namaExternal?: string,
  reportDisplay?: string,
  statusEnabled?: true,
  version?: 0,

}
