import { EventEmitter } from '@angular/core';

export interface KelompokUser {

  kode?: any,
  kodeExternal?: string,
  namaExternal?: string,
  namaKelompokUser?: string,
  reportDisplay?: string,
  statusEnabled?: true,
  version?: 0,

}
